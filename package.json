{
  "name": "microscope",
  "version": "0.0.1",
  "private": true,
  "description": "TypeScript SDK for end-to-end observability for AI workloads",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "vitest": "^3.2.3",
    "@vitest/coverage-v8": "3.2.3"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "libs/*",
    "sdk/*"
  ]
}