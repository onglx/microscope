{
  "name": "@microscope/infra",
  "version": "0.0.1",
  "private": true,
  "description": "Infrastructure code for Microscope SDK",
  "scripts": {
    "build": "tsc",
    "deploy": "pulumi up",
    "destroy": "pulumi destroy",
    "lint": "eslint ."
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/kubernetes": "^4.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}